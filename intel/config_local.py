"""
Local config overrides — this file is gitignored, never commit it.
Copy from intel/config.py and add your secrets here.
"""
import os

# Set your Anthropic API key here OR via environment variable
# os.environ["ANTHROPIC_API_KEY"] = "sk-ant-..."

# Optionally override other config values:
# CLAUDE_MODEL = "claude-sonnet-4-20250514"
